# 🛸 Drone Object Detection System (End-to-End Pipeline)

Это комплексное решение для обнаружения объектов (в частности, солдат) с помощью беспилотных летательных аппаратов (БПЛА). Проект включает в себя полный цикл: от предобработки сырого видео до обучения нейросети YOLO и развертывания веб-сервиса с современным интерфейсом.

## 🏗 Структура проекта

Проект разделен на две основные части: **ML Pipeline** (разработка модели) и **Web App** (интерфейс пользователя).

```bash
full_cv_drone/
├── cv_pipeline/            # === Среда разработки и обучения нейросети ===
│   ├── config/             # Файлы конфигурации (config.yaml)
│   ├── data/               # Данные: сырые видео, кадры, авторазметка, датасеты
│   ├── models/             # Базовые и предобученные веса (.pt)
│   ├── scripts/            # Модули: labeller, trainer, deduplicator
│   ├── outputs/            # Результаты тренировок и экспорта
│   └── main_pipeline.ipynb # Основной пульт управления всем процессом
└── cv_site/                # === Веб-интерфейс для работы с моделью ===
    ├── backend/            # FastAPI сервер (Python)
    │   ├── model/          # Место для лучших весов (.pt / .onnx)
    │   └── main.py         # Эндпоинты для загрузки фото/видео
    └── frontend/           # Клиентская часть (React + Vite + Tailwind)

```

---

## 🛠 1. ML Pipeline (cv_pipeline)

Этот блок предназначен для подготовки данных и обучения модели. Управление осуществляется через `main_pipeline.ipynb`.

### Ключевые возможности:

1. **Preprocessing**: Автоматическая нарезка видео на кадры с заданным FPS и удаление визуальных дубликатов (используется `pHash` дедупликация).
2. **Auto-Labeling**: Разметка новых данных существующей моделью.
* **CVAT Integration**: Скрипт генерирует `archive.zip` в формате YOLO 1.1, который можно напрямую импортировать в CVAT для ручной корректировки.


3. **Smart Split**: Автоматическое разделение данных на `train`, `val` и `test` согласно пропорциям в конфиге.
4. **Training**: Интеграция с библиотекой Ultralytics для обучения YOLOv8/v11.

### Настройка через `config.yaml`:

Вы можете менять любые параметры без правки кода:

* **Пути**: Настройка папок для видео, кадров и аннотаций.
* **Параметры дедупликации**: `hash_threshold` (чувствительность к изменениям).
* **Параметры обучения**: `epochs`, `imgsz` (размер картинки), `patience` (ранняя остановка).
* **Разметка**: `confidence_threshold` для автоматического фильтра объектов.

---

## 🌐 2. Web Application (cv_site)

Позволяет пользователям загружать файлы для инференса через браузер.

### Backend (FastAPI)

* **Гибридный детектор**: Поддерживает работу как с обычными весами PyTorch (`.pt`), так и с оптимизированным форматом `ONNX` для CPU-серверов.
* **Обработка видео**: Включает автоматическое сжатие через `ffmpeg` для быстрой отдачи результата клиенту.
* **Безопасность**: Ограничение размера файлов (до 50 МБ) и валидация расширений.

### Frontend (React)

* Современный стек на базе **Vite** и **Tailwind CSS**.
* Загрузка файлов методом Drag-and-Drop.
* Отображение результатов детекции прямо в браузере.

---

## 🚀 Инструкция по запуску

### Шаг 1: Обучение модели

1. Положите сырые видео в `cv_pipeline/data/raw_data/downloaded_videos`.
2. Настройте `cv_pipeline/config/config.yaml`.
3. Последовательно выполните ячейки в `main_pipeline.ipynb`.
4. Лучшая модель сохранится в `cv_pipeline/outputs/train_.../weights/best.pt`.

### Шаг 2: Запуск веб-интерфейса
1. **Backend**:
!!!
Проект **в разработке**, в данный момент он **не поддерживает** работу с ONNX, поэтому **используем** PyTorch.
Также в бекенде **много заглушек**, которые со временем будут удалены. Сейчас он **поддерживает** только одну модель.

Приступаем:

Прежде всего откройте backend/main.py

В блоке:

```bash
# --- CORS ---
app = FastAPI()
app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:5173",
        "http://ваш-домен",
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

Добавьте в список `allow_origins` ваш IP-адрес
(вы можете узнать его, например, через команду `ipconfig` в Windows или во время запуска фронтенда чуть дальше).

По умолчанию `http://localhost:5173` должен работать, но все же.

Затем в блоке:

```bash
MODEL_PATHS = {
    "nano": "models/model_nano.pt",
    "small": "models/model_small.pt",
    "medium": "models/model_medium.pt",
    "large": "models/model_large.pt",
}
```

Измените необходимую модель на конкретное название вашей модели в папке `cv_site/backend/models/`.
!!!
**Временно внедрен блок, именно тут меняйте свою модель:**

```bash
PT_MODELS = {
    "small": PTDetector("models/model_small.pt"),
    # Можно добавить другие модели, если потребуется
}
```



```bash
cd cv_site/backend
# Скопируйте обученную модель в папку models/
# Если папки models нету, создайте её

# проверьте наличие виртуального окружения (я использую conda, вы можете использовать venv)
# python -m venv venv
# venv\Scripts\activate
pip install -r requirements.txt

# запуск бекенда
uvicorn main:app --reload
# или (доступен по всем устройствам в локальной сети W-Fi)
uvicorn main:app --host 0.0.0.0 --port 8000 --reload

```


2. **Frontend**:
```bash
cd cv_site/frontend
# установка зависимостей
npm install
# запуск фронтенда
npm run dev
# или (доступен по всем устройствам в локальной сети W-Fi)
npm run dev -- --host 0.0.0.0 --port 5173
```



---

## 📦 Зависимости

Основные инструменты, используемые в проекте:

* **Python 3.10+**
* **Ultralytics (YOLO)**: для обучения и инференса.
* **OpenCV**: для обработки видеопотоков.
* **FastAPI**: для бэкенда.
* **React + Vite**: для фронтенда.
* **FFmpeg**: для кодирования видео.

---

## 📝 Важное примечание по CVAT

При автоматической разметке (`auto_labeller.py`) в папке `data/auto_annotations` создается файл **`archive.zip`**.
Для загрузки в CVAT:

1. Создайте Task.
2. Нажмите "Upload Annotations".
3. Выберите формат **YOLO 1.1** и загрузите этот архив. Все кадры и рамки появятся в интерфейсе разметки автоматически.
